/**
 * Simulación de llenado: Siguiendo el ejemplo de las fuentes, simula el llenado de un contenedor imprimiendo "Manzana añadida: X" hasta llegar a 100 manzanas
 */

let contador = 1;

while (contador <= 100) {
    console.log("Manzana añadida: " + contador);
    contador++;
}